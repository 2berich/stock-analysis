server:
  port: 8000

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1/stock?charactorEncoding=utf-8&useSSL=false
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

stock:
  config:
    times: 3
    threshold: 4000
    profitLimit: 5